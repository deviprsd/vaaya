<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vaaya.shraavani.vaaya">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".master.VaayaApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/VaayaTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/VaayaTheme.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/title_activity_welcome"
            android:theme="@style/VaayaTheme.Welcome"
            android:noHistory="true" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="25a8c3aa6fabb7abe1ead3ed65d7b7a0bf804235" />

        <activity android:name=".HomeActivity" />
    </application>

</manifest>